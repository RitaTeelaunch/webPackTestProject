export interface TestType{
    message: string;
}

const styles: {
    subContainer: string;
    button: string;
    buttonClicked: string;
    input: string;
    inputGroup: string;
} = require('./Test.module.css');

export default styles;